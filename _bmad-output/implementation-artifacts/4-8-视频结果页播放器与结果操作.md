# Story 4.8: 视频结果页、播放器与结果操作

Status: backlog

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 获得结果的用户，
I want 在结果页直接播放讲解视频并执行基础控制与结果操作，
so that 我可以立刻完成一次完整的单题复习体验。

## Acceptance Criteria

1. 视频任务完成后，用户进入 `/video/:id` 时，页面查询并展示视频播放器（主区域）、题目摘要、知识点摘要、AI 内容标识，用户不需要跳到其他工具或临时链接才能观看结果。
2. 播放器基于 Video.js 封装，支持播放、暂停、倍速切换（0.5x / 1x / 1.5x / 2x）、拖动进度条与全屏控制，这些操作立即生效并反馈到 UI；从历史记录再次打开结果页也能稳定消费同一套播放器行为。
3. 页面在 mock 模式下可展示完成态、视频缺失态（URL 失效）、权限失败态（非本人任务）与加载失败态（API 异常），不需要真实视频流水线跑通后才开始实现页面结构和状态机。
4. `Companion`、`Evidence / Retrieval` 与 `Learning Coach` 仅作为后续动作入口（侧边栏按钮或底部 CTA），不抢占播放器的主叙事中心位置；入口按钮在对应 Epic 未实现前展示 `disabled` 或 `coming soon` 状态。
5. 结果操作区包含公开发布/取消公开按钮、结果复用入口（"使用此题目重新生成"）与分享入口（复制链接），即使当前成品图未完整展开操作区，仍必须按 Story 4.10 约定预留完整操作区壳层。
6. 结果页数据通过 `@tanstack/react-query` 的 `useQuery` 获取，支持缓存与重新验证；结果加载中展示 skeleton 而非空白。

## Tasks / Subtasks

- [ ] 实现结果页数据获取与状态管理（AC: 1, 3, 6）
  - [ ] 在 `packages/student-web/src/features/video/hooks/` 下创建 `useVideoResult.ts`。
  - [ ] 使用 `useQuery` 调用 `/api/v1/video/tasks/:id` 获取 `VideoResult`。
  - [ ] 处理加载中（skeleton）、成功（渲染结果）、视频缺失（URL 失效提示）、权限失败（403 → 提示非本人任务）、加载失败（API 异常 → 重试按钮）。
  - [ ] 定义前端 `VideoResultView` 类型，映射后端 `VideoResult` schema。
- [ ] 实现 Video.js 播放器封装（AC: 2）
  - [ ] 在 `packages/student-web/src/features/video/components/` 下创建 `video-player.tsx`。
  - [ ] 封装 Video.js 初始化、销毁与配置管理。
  - [ ] 支持倍速切换（0.5x / 1x / 1.5x / 2x）、全屏、进度条拖动。
  - [ ] 播放器样式通过 feature SCSS 定制，覆盖 Video.js 默认皮肤以对齐设计系统。
  - [ ] 播放器组件接收 `videoUrl` prop，URL 变更时重新初始化。
  - [ ] 组件卸载时正确销毁 Video.js 实例，防止内存泄漏。
- [ ] 实现结果页主布局（AC: 1, 4, 5）
  - [ ] 创建 `packages/student-web/src/features/video/pages/video-result-page.tsx`。
  - [ ] 主布局：左侧播放器 + 右侧/底部信息区（题目摘要、知识点列表、AI 内容标识）。
  - [ ] 后续动作入口区：Companion（disabled）、Evidence（disabled）、Learning Coach（disabled），按钮带 tooltip "即将上线"。
  - [ ] 结果操作区壳层：公开发布/取消公开按钮、"使用此题目重新生成"按钮、"复制链接"按钮。
  - [ ] AI 内容标识：在播放器下方或信息区顶部展示 `🤖 AI 生成内容` 标识。
- [ ] 实现结果操作交互（AC: 5）
  - [ ] 公开发布/取消公开：调用 Story 4.10 定义的 API（本 Story 预留 adapter 占位，mock handler 返回成功）。
  - [ ] 使用 `useMutation` 管理发布/取消操作状态。
  - [ ] "使用此题目重新生成"：`navigate` 到 `/video/input?reuseTaskId=:id`，输入页预填充题目内容。
  - [ ] "复制链接"：复制当前页面 URL 到剪贴板，使用 `sonner` toast 提示"已复制"。
- [ ] 实现 mock 模式支持（AC: 3）
  - [ ] 扩展视频 mock handler，支持 `GET /api/v1/video/tasks/:id` 返回 mock VideoResult。
  - [ ] mock handler 支持多种场景：正常完成、视频 URL 失效、权限失败。
  - [ ] 提供 mock 视频 URL（使用公开测试视频地址或本地 static asset）。
- [ ] 实现样式（AC: 1, 2, 4）
  - [ ] 创建 `packages/student-web/src/features/video/styles/_result.scss`。
  - [ ] 播放器区域响应式布局（桌面：左播放器 + 右信息；移动：上播放器 + 下信息）。
  - [ ] Video.js 皮肤定制：进度条颜色、控制栏样式、全屏按钮对齐设计系统。
  - [ ] 使用 `--xm-*` 令牌，不硬编码颜色和间距。
- [ ] 建立测试（AC: 1, 2, 3, 4, 5, 6）
  - [ ] `useVideoResult` hook 测试：loading → skeleton、success → 结果、error → 重试。
  - [ ] 播放器组件测试：初始化与销毁、props 变更重新初始化。
  - [ ] 结果页渲染测试：完成态、缺失态、权限失败态的 UI 快照。
  - [ ] 结果操作测试：复制链接 → clipboard API、重新生成 → navigate 正确路由。
  - [ ] 后续入口测试：disabled 状态下按钮不可点击。

## Dev Notes

### Story Metadata

- Story ID: `4.8`
- Story Type: `Frontend Story`
- Epic: `Epic 4`
- Depends On: `4.1`（VideoResult schema）、`3.5`（等待页成功后跳转目标）、`2.5`（SSE 事件契约，task:completed 消费）
- Blocks: `4.10`（公开发布交互基于结果页操作区）、`6.2`（Companion 侧栏挂载在结果页）、`7.2`（Evidence 面板挂载在结果页）
- Contract Asset Path: 消费 `contracts/video/v1/video-result.schema.json`
- Mock Asset Path: 消费 `mocks/video/video-result.success.json`
- API / Event / Schema Impact: 调用 `GET /api/v1/video/tasks/:id`；消费 VideoResult schema
- Persistence Impact: 无
- Frontend States Covered: 加载中（skeleton）、完成态、视频缺失态、权限失败态、加载失败态
- Error States Covered: 视频 URL 失效、API 异常、权限不足（非本人任务）
- Acceptance Test Notes: 必须覆盖五种页面状态、播放器基础控制、结果操作交互

### Business Context

- 结果页是视频生成主链路的"最终消费端"，用户在这里完成视频观看和后续动作。
- 播放器是页面的绝对主角，Companion/Evidence/Learning Coach 只是辅助入口，不能喧宾夺主。
- 公开发布操作是 Story 4.10 的核心交互入口，本 Story 必须预留完整操作区壳层而非事后补丁。
- AI 内容标识是合规要求，不可省略或隐藏。
- Video.js 已在 `package.json` 中引入，是架构文档指定的视频播放解决方案。

### Technical Guardrails

- Video.js 封装必须处理组件卸载时的实例销毁（`player.dispose()`），避免内存泄漏。
- Video.js 初始化必须在 DOM 挂载后（`useEffect` + ref），不在 render 阶段调用。
- 结果数据使用 `useQuery` + `staleTime: 5min`，避免每次路由切换重新请求。
- 公开发布/取消操作使用 `useMutation` + `onSuccess` 刷新 `useQuery` 缓存。
- 复制链接使用 `navigator.clipboard.writeText()`，降级到 `document.execCommand('copy')`。
- 页面级样式使用 feature SCSS，播放器皮肤覆盖集中在 `_result.scss` 中。
- 结果页路由 `/video/:id` 为受保护路由，未登录 → 跳登录页，非本人任务 → 展示 403 提示。
- 后续动作入口按钮的 disabled 状态应由 feature flag 或 Epic 完成状态控制，而非硬编码。

### Suggested File Targets

- `packages/student-web/src/features/video/pages/video-result-page.tsx`
- `packages/student-web/src/features/video/components/video-player.tsx`
- `packages/student-web/src/features/video/components/result-summary-card.tsx`
- `packages/student-web/src/features/video/components/result-actions-bar.tsx`
- `packages/student-web/src/features/video/components/future-actions-bar.tsx`
- `packages/student-web/src/features/video/hooks/useVideoResult.ts`
- `packages/student-web/src/features/video/styles/_result.scss`
- `packages/student-web/src/features/video/styles/_video-player.scss`
- `packages/student-web/src/services/mock/handlers/video-task.ts`（扩展 GET result mock）
- `packages/student-web/src/test/features/video/video-result-page.test.tsx`
- `packages/student-web/src/test/features/video/video-player.test.tsx`

### Project Structure Notes

- `pages/video-result-page.tsx` 为新建文件，是 `/video/:id` 路由的页面容器。
- `components/` 下新增播放器、摘要卡片、操作栏等子组件，从页面容器解耦。
- `hooks/useVideoResult.ts` 使用 `@tanstack/react-query`，与 `useVideoSSE.ts` 平级。
- `styles/` 下新增 `_result.scss` 和 `_video-player.scss`，分别管理结果页布局和播放器皮肤。

### Testing Requirements

- `useVideoResult` hook 测试：loading 状态 → skeleton 展示；success → 数据正确映射；error → 重试按钮出现。
- 播放器组件测试：渲染后 Video.js 实例创建；卸载后 `dispose` 调用；`videoUrl` 变更后重新初始化。
- 结果页集成测试：完成态展示播放器 + 摘要 + 操作区；缺失态展示"视频不可用"提示；权限失败态展示 403。
- 结果操作测试："复制链接" → `navigator.clipboard.writeText` 调用；"重新生成" → `navigate` 到 `/video/input?reuseTaskId=:id`。
- 后续入口测试：Companion/Evidence/Learning Coach 按钮 `disabled`，点击无反应。
- AI 内容标识测试：`aiContentFlag: true` → 标识展示。
- 响应式测试：桌面宽屏和移动窄屏的布局切换。

### References

- `_bmad-output/planning-artifacts/epics/17-epic-4.md`：Epic 4 范围、Story 4.8 AC 与 Deliverables。
- `_bmad-output/implementation-artifacts/4-1-视频流水线阶段进度区间与结果契约冻结.md`：VideoResult schema。
- `_bmad-output/implementation-artifacts/3-5-创建后跳转等待页与任务上下文承接.md`：等待页跳转目标。
- `_bmad-output/planning-artifacts/ux-design-specification/08-7-page-level-design-specifications页面级设计规范.md`：视频结果页设计规范。
- `docs/03UI:UX 设计素材/001UI 设计稿/04-成品图/01-正式路由页面/05-视频结果页/02-video-result.html`：结果页成品图。
- `_bmad-output/planning-artifacts/prd/06-6-功能需求.md`：`FR-VP-001`、`FR-VP-002`、`FR-VP-003`、`FR-VP-004`。
