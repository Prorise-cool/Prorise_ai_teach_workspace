# Plan D 编排与契约收口记录（2026-04-10）

## 背景

本次收口针对 Plan D Phase 1-6 之后仍残留的真实联调问题，重点不是继续扩展能力，而是把已经落地的 `solve / storyboard / render_verify` 主链路重新对齐到可执行、可观察、可复现的状态。

## 本次收口内容

### 1. 后端编排

- `understanding -> solve -> storyboard` 重新固定为独立阶段串联。
- 取消 orchestrator 对 merged storyboard 的默认捷径依赖，避免 `solve` 已存在但 `storyboard` 被提前跳过。
- `render_verify` 保留新服务实现，但重新发出 `render` / `manim_fix` 事件语义，保证等待页与 SSE 观察面不丢阶段上下文。

### 2. 前端契约

- 补齐 `solve` / `render_verify` 的阶段文案。
- 补齐 `VIDEO_SOLVE_FAILED` 的类型声明、i18n 文案和统一错误映射。
- 等待页现有 `manim_fix` 修复态 UI 继续工作，不需要额外兼容层。

### 3. SQL 迁移

- `20260410_plan_d_stage_bindings.sql` 改为按 `module_code / provider_code / resource_code` 动态解析主键。
- 迁移不再假设固定 DeepSeek provider/resource ID 已存在。
- Provider 更新不覆盖真实鉴权字段，避免迁移把后台已配置的凭据写回占位值。

## 验证

- `pytest -q packages/fastapi-backend/tests/unit/video/` -> `81 passed`
- `pnpm --dir packages/student-web typecheck` -> passed
- `pnpm --dir packages/student-web test -- src/features/video/pages/video-generating-page.test.tsx` -> `32 files, 184 tests passed`

## 仍需人工联调

- 将 `packages/RuoYi-Vue-Plus-5.X/script/sql/update/20260410_plan_d_stage_bindings.sql` 执行到开发库。
- 手动创建一道视频任务，确认 SSE 中依次出现 `solve`、`storyboard`、`render` / `manim_fix`、`render_verify`。
- 验证等待页文案、失败态和最终结果页表现与后端真实事件一致。
