# Story 3.6: 视频输入页公开视频广场与复用入口

Status: backlog

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 想快速判断是否已有相近讲解的用户，
I want 在视频输入页浏览公开视频卡片并把合适内容复用到当前输入流程，
so that 我可以先看已有结果或少走重复创建路径。

## Acceptance Criteria

1. `/video/input` 页面在核心输入区下方展示公开视频卡片列表或推荐集合，主输入区与主 CTA 仍保持页面主叙事位置，公开内容区不抢占核心输入区的视觉权重。
2. 公开视频卡片至少展示缩略图、题目摘要、时长、观看次数（或热度指标），用户可快速扫读判断是否有需要的内容。
3. 用户点击卡片上的"查看讲解"可进入对应公开视频结果页（`/video/:id`）；点击"复用题目"可将该视频的题目文本或摘要带回当前输入区，自动填充到 textarea 中。
4. 公开内容为空时展示空态插画与提示文案（"暂无公开视频，快来创建第一个"）；加载失败时展示降级提示但不阻断主输入流程；mock 模式下使用 mock 卡片数据。
5. 公开视频数据通过 `GET /api/v1/video/public` 获取，使用 `useQuery` 管理缓存与刷新；接口返回分页列表，首次加载展示一屏（6-12 张卡片）。
6. 公开视频发现区的加载失败、空态、骨架屏均不影响核心输入区的可用性——用户始终可以直接输入题目并创建新视频任务。
7. 卡片布局使用响应式网格，桌面端 3-4 列，配合已有的 `CommunityFeed` 组件或升级其数据源。

## Tasks / Subtasks

- [ ] 定义公开视频列表接口契约（AC: 5）
  - [ ] 在 `contracts/video/v1/` 下新增 `public-videos.schema.json`，定义请求参数（page、pageSize、sort）与响应结构 `{ items: VideoPublicCard[], total, page, pageSize }`。
  - [ ] `VideoPublicCard` 字段：`videoId`、`title`、`thumbnail`、`duration`、`viewCount`、`createdAt`、`sourceText`（用于复用）。
  - [ ] mock 样例：`mocks/video/public-videos.json`。
- [ ] 升级或对接 CommunityFeed 组件（AC: 1, 7）
  - [ ] 评估现有 `CommunityFeed` 组件是否可直接承接公开视频数据源。
  - [ ] 若 `CommunityFeed` 已满足卡片布局需求，替换其 mock 数据源为 `useQuery` + adapter。
  - [ ] 若需定制化（如"复用题目"动作），拆出 `VideoPublicCard` 子组件。
- [ ] 实现公开视频数据层（AC: 5）
  - [ ] 新建 `src/services/api/adapters/video-public-adapter.ts`，封装 `fetchPublicVideos(params) -> PublicVideoListResponse`。
  - [ ] 使用 `useQuery` 管理缓存（staleTime: 5min），首次加载默认 page=1, pageSize=12。
  - [ ] mock handler 注册 `GET /api/v1/video/public`。
- [ ] 实现"查看讲解"动作（AC: 3）
  - [ ] 卡片点击或"查看讲解"按钮导航到 `/video/:videoId`。
  - [ ] 使用 `react-router-dom` `Link` 或 `navigate`，在新标签页或当前页打开（MVP 当前页即可）。
- [ ] 实现"复用题目"动作（AC: 3）
  - [ ] "复用题目"按钮点击后，将 `sourceText` 填充到输入页 textarea（通过 `react-hook-form` `setValue`）。
  - [ ] 填充后自动滚动到输入区并 focus textarea。
  - [ ] 用 `sonner` toast 提示"已复用题目到输入区"。
- [ ] 实现空态、错态与骨架屏（AC: 4, 6）
  - [ ] 空态：空列表时展示插画 + 提示文案。
  - [ ] 错态：加载失败时展示降级提示（"公开视频暂时不可用"），不影响输入区。
  - [ ] 骨架屏：首次加载时展示 6-12 个卡片骨架。
  - [ ] 所有异常态均不阻断主输入流程。
- [ ] 样式对齐（AC: 1, 7）
  - [ ] 公开视频区在输入区下方，视觉权重低于核心输入区。
  - [ ] 使用 feature SCSS（`video-input-page.scss` 或新建 partial）。
  - [ ] 暗黑模式支持。
  - [ ] 响应式：桌面 3-4 列，窄屏 2 列。
- [ ] 测试覆盖（AC: 1–7）
  - [ ] 覆盖公开视频正常加载并渲染卡片。
  - [ ] 覆盖空态展示。
  - [ ] 覆盖加载失败展示降级提示且输入区可用。
  - [ ] 覆盖"复用题目"填充到 textarea。
  - [ ] 覆盖"查看讲解"导航到正确路由。

## Dev Notes

### Story Metadata

- Story ID: `3.6`
- Story Type: `Frontend Story`
- Epic: `Epic 3`
- Depends On: `3.1`（视频域契约基线）、`3.2`（输入页壳层已就绪）
- Blocks: `4.10`（视频结果公开发布与输入页复用卡片，需复用本 Story 的卡片组件）
- Contract Asset Path: `contracts/video/v1/`
- Mock Asset Path: `mocks/video/`
- API / Event / Schema Impact: 新增 `GET /api/v1/video/public` 契约
- Persistence Impact: 无（只读消费）
- Frontend States Covered: 加载中（骨架屏）、正常展示、空态、错态、mock 态
- Error States Covered: 网络错误、接口超时、空列表、mock 模式
- Acceptance Test Notes: 必须覆盖正常加载、空态、错态三条路径，并验证失败不阻断主输入流程

### Business Context

- 公开视频发现区是"视频输入页的辅助层"——帮助用户快速判断"是否已有人生成过类似题目的讲解"。这是降低用户重复创建成本的核心体验。
- 但公开视频区的优先级低于核心输入区。Epic 3 Frontend Design Reference 明确规定"公开视频发现区属于辅助理解与复用层，不得让其抢占核心输入区与主 CTA 的主叙事位置"。
- 当前输入页已有 `CommunityFeed` 组件渲染 mock 卡片（`VIDEO_FEED_MOCK_CARDS`），本 Story 需将其升级为真实数据源驱动，或在需要时替换为更适合视频域的卡片组件。
- "复用题目"是一个低频但高价值的操作——当用户发现已有类似讲解时，可以直接复用其题目文本，节省输入成本。

### Technical Guardrails

- 公开视频列表使用 `@tanstack/react-query` 的 `useQuery` 管理，不手写请求状态机。
- 公开视频数据的加载失败必须被隔离——不能让 API 失败导致整个输入页白屏或不可用。使用 React Error Boundary 或 `useQuery` 的 `isError` 状态在局部区域降级。
- "复用题目"通过 `react-hook-form` `setValue` 设置 textarea 值，不绕过表单管理直接操作 DOM。
- 卡片布局如果复用 `CommunityFeed`，需确保其数据接口可泛化（不仅限于 mock 数据）。若 `CommunityFeed` 的接口设计过于刚性，可新建 `VideoPublicFeed` 组件。
- 公开视频的后端接口（`GET /api/v1/video/public`）在本 Story 中只定义契约和 mock，真实后端实现由 Story 4.10 负责。
- 所有 `className` 通过 `cn()` 合并，卡片变体如需定制使用 CVA。

### Suggested File Targets

- `contracts/video/v1/public-videos.schema.json`（新建）
- `mocks/video/public-videos.json`（新建）
- `mocks/video/public-videos.empty.json`（新建）
- `packages/student-web/src/services/api/adapters/video-public-adapter.ts`（新建）
- `packages/student-web/src/features/video/hooks/use-public-videos.ts`（新建，封装 useQuery）
- `packages/student-web/src/features/video/components/video-public-feed.tsx`（新建或升级 CommunityFeed）
- `packages/student-web/src/features/video/components/video-public-card.tsx`（新建）
- `packages/student-web/src/features/video/pages/video-input-page.tsx`（更新，挂载公开视频区）
- `packages/student-web/src/features/video/styles/partials/_video-public-feed.scss`（新建）
- `packages/student-web/src/services/mock/handlers/video-task.ts`（扩展，添加 public videos mock）
- `packages/student-web/src/types/video.ts`（扩展，添加 VideoPublicCard 类型）
- `packages/student-web/src/test/features/video/video-public-feed.test.tsx`（新建）

### Project Structure Notes

- 当前 `video-input-page.tsx` 底部已挂载 `<CommunityFeed>` 组件，使用 `VIDEO_FEED_MOCK_CARDS` mock 数据。本 Story 需评估是将 `CommunityFeed` 升级为可接入真实数据源，还是新建 `VideoPublicFeed` 组件。
- `packages/student-web/src/components/community-feed/` 下已有 `CommunityFeed` 组件实现，包含卡片网格布局、分类标签、加载更多等功能。
- `packages/student-web/src/types/video.ts` 在 Story 3.1 中创建，本 Story 需追加 `VideoPublicCard` 类型定义。

### Testing Requirements

- 覆盖公开视频 API 正常返回时卡片渲染数量与内容正确。
- 覆盖空列表返回时展示空态 UI。
- 覆盖 API 错误时展示降级提示且输入区仍可操作。
- 覆盖"复用题目"按钮点击后 textarea 值更新。
- 覆盖"查看讲解"按钮点击后导航到正确路径。
- 覆盖 mock 模式下使用 mock 数据正常渲染。
- 覆盖骨架屏在加载中状态展示。

### References

- `_bmad-output/planning-artifacts/epics/16-epic-3.md`：Story 3.6 AC 与 Deliverables。
- `_bmad-output/planning-artifacts/prd/06-6-功能需求.md`：`FR-VS-010`（公开视频卡片、查看/复用、空态/错态不阻断）。
- `_bmad-output/planning-artifacts/ux-design-specification/08-7-page-level-design-specifications页面级设计规范.md`：视频输入页公开视频发现区规范。
- `_bmad-output/implementation-artifacts/3-1-视频任务创建契约与-mock-task-基线.md`：视频域契约基线。
- `_bmad-output/implementation-artifacts/3-2-视频输入页壳层与多模态输入交互.md`：输入页壳层。
- `_bmad-output/implementation-artifacts/4-10-视频结果公开发布与输入页复用卡片.md`：后续消费本 Story 卡片组件。
- `docs/03UI:UX 设计素材/001UI 设计稿/04-成品图/01-正式路由页面/03-视频输入页/01-input.html`：成品图参考。
