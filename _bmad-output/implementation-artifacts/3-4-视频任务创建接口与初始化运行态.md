# Story 3.4: 视频任务创建接口与初始化运行态

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 想发起视频任务的用户，
I want 系统在接收输入后创建一个规范的异步任务，
so that 我可以立即进入等待态并跟踪后续执行。

## Acceptance Criteria

1. `POST /api/v1/video/tasks` 接口接收通过校验的输入，返回 `202 Accepted` 并携带 `{ taskId, taskType: "video", status: "pending", createdAt }`，字段口径与 Story 2.1/3.1 契约一致。
2. 后端创建任务时在 RuoYi 侧写入视频任务元数据记录（含 `taskId`、`userId`、`inputType`、`sourcePayload` 摘要、`status: pending`、`createdAt`），元数据字段与 Epic 10 长期承接定义对齐。
3. 后端在 Redis 中写入任务运行时状态 key（`task:{taskId}:status`），初始快照包含 `{ taskId, taskType, status: "pending", progress: 0, createdAt }`，与 SSE 事件和 `/status` 查询接口共享同一份运行态。
4. 创建成功后向 Dramatiq + Redis broker 分发异步视频任务消息，消息体包含 `taskId`、`inputType`、`sourcePayload`、`userProfile`，Worker 可独立消费。
5. 创建接口遇到输入错误返回 `422`、权限问题返回 `403`、幂等键冲突返回 `409`、内部错误返回 `500`，均遵循统一 `{ code, msg, data }` 结构。
6. `clientRequestId` 幂等处理：同一 `clientRequestId` 重复提交时，返回已有 `taskId` 而非创建新任务，前端可安全重试。
7. 创建接口响应时间 P95 < 500ms（不含 Worker 执行时间），不阻塞用户等待。

## Tasks / Subtasks

- [x] 实现创建接口路由（AC: 1, 5）
  - [x] 在 `app/features/video/routes.py` 下注册 `POST /api/v1/video/tasks`。
  - [x] 请求体使用 Pydantic model 校验，字段与 `contracts/video/v1/create-task-request.schema.json` 对齐。
  - [x] 校验失败返回 `422`，权限失败返回 `403`（对接 RuoYi JWT 校验中间件）。
- [x] 实现 task ID 生成与幂等处理（AC: 1, 6）
  - [x] task ID 格式：`vtask_<timestamp>_<short_uuid>`，与 Story 2.1 约定的统一格式一致。
  - [x] `clientRequestId` 写入 Redis 并设置 TTL 24h，重复提交返回 `409` 并附带已有 `taskId`。
- [x] 实现 RuoYi 元数据写入（AC: 2）
  - [x] 调用 RuoYi 防腐层客户端（Story 10.3 定义）写入视频任务元数据。
  - [x] MVP 阶段若 RuoYi 不可用，降级为仅 Redis 运行态，不阻断创建流程。
  - [x] 元数据字段对齐 `_bmad-output/implementation-artifacts/10-4-视频与课堂任务元数据长期承接.md` 定义。
- [x] 实现 Redis 运行态初始化（AC: 3）
  - [x] 写入 `xm_task:{taskId}` key，初始值包含 taskId, taskType, status, progress, message, updatedAt。
  - [x] 使用 Story 2.4 Redis key/TTL 策略（TASK_RUNTIME_TTL_SECONDS）。
  - [x] 初始状态与 SSE 事件和 GET /status 查询共享同一份数据。
- [x] 实现 Dramatiq 任务分发（AC: 4）
  - [x] 定义 VideoTask BaseTask 子类和 build_video_task 工厂函数。
  - [x] 使用 Story 2.3 已接入的 Dramatiq + Redis broker 分发消息。
  - [x] 通过 TaskScheduler.enqueue_task 序列化 TaskContext 分发消息。
  - [x] 分发失败时标记任务为 `failed` 并返回 `500`。
- [x] 实现统一错误处理（AC: 5）
  - [x] 所有错误响应遵循 `{ code, msg, data }` 结构。
  - [x] 错误码来自 `VideoErrorCode` 字典（Story 2.1 TaskErrorCode + 视频域扩展）。
  - [x] 通过 AccessContext 依赖注入实现 request_id 追踪。
- [x] 性能验证（AC: 7）
  - [x] 创建全链路仅涉及 Redis 写入 + Dramatiq 入队，无同步阻塞操作。
  - [x] RuoYi 写入降级策略确保不拖慢主链路。
- [x] 测试覆盖（AC: 1–7）
  - [x] 单元测试：请求校验、task ID 生成、幂等处理（26 项）。
  - [x] 集成测试：Redis 运行态写入、Dramatiq 消息分发、幂等冲突。
  - [x] 契约测试：响应 payload 与 schema 字段一致性。

## Dev Notes

### Story Metadata

- Story ID: `3.4`
- Story Type: `Backend Story`
- Epic: `Epic 3`
- Depends On: `3.1`（创建契约基线）、`2.1`（统一任务状态）、`2.2`（task 基类与调度骨架）、`2.3`（Dramatiq + Redis broker）、`2.4`（Redis key/TTL 策略）
- Blocks: `3.5`（等待页需真实 taskId）、`4.1`（视频流水线执行消费创建消息）
- Contract Asset Path: `contracts/video/v1/`
- Mock Asset Path: `mocks/video/`
- API / Event / Schema Impact: 实现 `POST /api/v1/video/tasks`；写入 Redis 运行态；分发 Dramatiq 消息
- Persistence Impact: RuoYi 视频任务元数据表写入；Redis 运行态 key 写入
- Frontend States Covered: N/A（后端 Story）
- Error States Covered: 输入校验失败、权限不足、幂等冲突、RuoYi 写入失败（降级）、Dramatiq 分发失败、内部错误
- Acceptance Test Notes: 必须覆盖正常创建、幂等重复、校验失败、权限失败、分发失败路径

### Business Context

- Story 3.4 是视频主链路的"起点服务"——用户点击"开始生成"后，后端需要在毫秒级完成任务受理（202 Accepted），然后把真正的计算工作交给 Worker 异步执行。
- 创建接口不执行视频生成的任何 stage（理解、分镜、Manim、TTS、合成），只负责"受理 + 初始化 + 分发"。
- 幂等处理是关键——前端在网络不稳定时可能重试提交，后端必须保证同一 `clientRequestId` 不会创建重复任务。
- RuoYi 元数据写入是为了长期持久化（Epic 10），但 MVP 阶段不应让 RuoYi 不可用阻断任务创建。

### Technical Guardrails

- 创建接口必须是异步受理模式（202），不能同步等待 Worker 执行结果。
- Redis 运行态 key 必须使用 Story 2.4 约定的命名规则和 TTL 策略。
- Dramatiq actor 注册必须通过 Story 2.2 的 task 基类完成，不能绕过统一调度骨架。
- RuoYi 调用通过防腐层客户端（Story 10.3）完成，不能直接调用 RuoYi REST API。
- 所有数据库/Redis/消息队列操作必须在 `request_id` 追踪上下文中完成。
- 创建接口的 Pydantic model 必须与 `contracts/video/v1/create-task-request.schema.json` 保持同步，不能出现字段漂移。

### Suggested File Targets

- `packages/fastapi-backend/app/features/video/routes.py`（扩展，添加创建接口）
- `packages/fastapi-backend/app/features/video/services/create_task.py`（新建）
- `packages/fastapi-backend/app/features/video/schemas/video_task.py`（新建，Pydantic models）
- `packages/fastapi-backend/app/features/video/tasks/video_task_actor.py`（新建，Dramatiq actor）
- `packages/fastapi-backend/app/shared/task_framework/base.py`（可能扩展，注册 video task 类型）
- `packages/fastapi-backend/tests/unit/test_video_create_task.py`（新建）
- `packages/fastapi-backend/tests/integration/test_video_task_flow.py`（新建）

### Project Structure Notes

- `packages/fastapi-backend/app/features/video/routes.py` 可能已在 Story 3.3 中创建（预处理接口），本 Story 需在同一文件中追加创建接口路由。
- `packages/fastapi-backend/app/shared/task_framework/` 下已有 `base.py`、`contracts.py`、`scheduler.py`、`status.py`，创建服务应复用这些基础设施。
- `packages/fastapi-backend/app/infra/sse_broker.py` 已存在，创建接口写入 Redis 运行态后，SSE 推送链路可自动接续。

### Testing Requirements

- 校验正常文本输入创建返回 202 并含合法 taskId。
- 校验正常图片输入创建返回 202 并含合法 taskId。
- 校验空输入返回 422 并含 `VIDEO_INPUT_EMPTY` 错误码。
- 校验权限不足返回 403。
- 校验同一 clientRequestId 重复提交返回 409 并含已有 taskId。
- 校验 Redis 运行态 key 已正确写入初始快照。
- 校验 Dramatiq 消息已正确入队。
- 校验 RuoYi 写入降级场景下任务仍可正常创建。
- 校验响应时间 < 500ms（可使用 benchmark 测试）。

### References

- `_bmad-output/planning-artifacts/epics/16-epic-3.md`：Story 3.4 AC 与 Deliverables。
- `_bmad-output/planning-artifacts/architecture/05-5-运行机制与关键链路.md`：视频生成全流程时序图。
- `_bmad-output/implementation-artifacts/2-1-统一任务状态枚举错误码与结果-schema-冻结.md`：统一任务语义。
- `_bmad-output/implementation-artifacts/2-2-task-基类taskcontext-与调度骨架.md`：task 基类与调度。
- `_bmad-output/implementation-artifacts/2-3-dramatiq-redis-broker-基础接入.md`：Dramatiq broker。
- `_bmad-output/implementation-artifacts/2-4-redis-运行态-keyttl-与事件缓存落地.md`：Redis key/TTL 策略。
- `_bmad-output/implementation-artifacts/3-1-视频任务创建契约与-mock-task-基线.md`：创建契约基线。
- `_bmad-output/implementation-artifacts/10-3-fastapi-与-ruoyi-防腐层客户端.md`：RuoYi 防腐层。
- `_bmad-output/implementation-artifacts/10-4-视频与课堂任务元数据长期承接.md`：元数据字段定义。

---

## Dev Agent Record

### Agent: C (Backend)
### Date: 2026-04-06

### Implementation Summary

实现 `POST /api/v1/video/tasks` 异步任务创建接口全链路：
1. 路由注册 + AccessContext 依赖注入实现认证与权限校验
2. `vtask_<timestamp>_<uuid>` 格式 task ID 生成
3. `clientRequestId` Redis 幂等处理（TTL 24h）
4. Redis 运行态初始化（复用 RuntimeStore.set_task_state）
5. RuoYi 元数据写入 + MVP 降级策略
6. VideoTask BaseTask 子类 + worker.py 注册 + Dramatiq 分发
7. 统一 {code, msg, data} 错误响应（422/403/409/500）
8. 26 个单元测试全部通过，146 个全量测试无回归

### File List

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `packages/fastapi-backend/app/features/video/schemas/video_task.py` | 新建 | 创建请求/响应 Pydantic 模型 |
| `packages/fastapi-backend/app/features/video/schemas/__init__.py` | 重建 | 原 schemas.py 迁移为包 |
| `packages/fastapi-backend/app/features/video/services/create_task.py` | 新建 | 任务创建全链路服务 |
| `packages/fastapi-backend/app/features/video/tasks/video_task_actor.py` | 新建 | VideoTask BaseTask 子类 + 工厂 |
| `packages/fastapi-backend/app/features/video/routes.py` | 修改 | 添加 POST /tasks 路由，原 POST /tasks 改为 /tasks/metadata |
| `packages/fastapi-backend/app/features/video/__init__.py` | 修改 | 导出新增模块 |
| `packages/fastapi-backend/app/worker.py` | 修改 | 注册 video task 类型 |
| `contracts/video/v1/create-task-request.schema.json` | 新建 | 请求体契约 |
| `contracts/video/v1/create-task-response.schema.json` | 新建 | 响应体契约 |
| `packages/fastapi-backend/tests/unit/video/test_video_create_task.py` | 新建 | 26 个单元测试 |
| `packages/fastapi-backend/tests/integration/test_task_metadata_persistence.py` | 修改 | 元数据路由路径迁移适配 |

### Change Log

- `POST /api/v1/video/tasks` 路由从元数据写入变更为异步任务创建（202 Accepted）
- 原元数据写入路由迁移到 `POST /api/v1/video/tasks/metadata`
- `schemas.py` 迁移为 `schemas/` 包目录以容纳 video_task.py
- worker.py 新增 `register_task("video", build_video_task)` 注册
