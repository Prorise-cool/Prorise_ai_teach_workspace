In references/shadcn-vue-landing-page, Vue SFCs use `<script setup lang="ts">`, local typed arrays for section content, Tailwind utility classes, shadcn-vue UI primitives, and lucide-vue icons. Naming is component-based with PascalCase filenames. Content is embedded inline in component arrays rather than externalized. The user-level instruction for this workspace emphasizes matching existing patterns, small changes, and Chinese responses from the agent.