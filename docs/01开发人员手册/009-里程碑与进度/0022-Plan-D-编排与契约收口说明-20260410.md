# 0022-Plan D 编排与契约收口说明-20260410

## 目的

本记录用于说明 2026-04-10 对视频管道 Plan D 的收口动作，重点解决“主链路真实执行”和“前后端等待态契约一致”两个问题。

## 收口结论

### 后端

- `understanding` 不再默认合并输出分镜，主编排固定走 `understanding -> solve -> storyboard`。
- `render_verify` 继续承担渲染修复循环，但恢复 `render` 与 `manim_fix` 的阶段事件，避免等待页只看到一个黑盒阶段。

### 前端

- 已补齐 `solve`、`render_verify` 阶段文案。
- 已补齐 `VIDEO_SOLVE_FAILED` 的错误码映射。

### 数据库迁移

- Plan D SQL 已改为按编码解析 `module / provider / resource` 主键，可从仓库快照初始化的开发库直接执行。

## 已完成验证

- 后端单测：`pytest -q packages/fastapi-backend/tests/unit/video/` -> `81 passed`
- 前端类型检查：`pnpm --dir packages/student-web typecheck` -> passed
- 前端等待页回归：`pnpm --dir packages/student-web test -- src/features/video/pages/video-generating-page.test.tsx` -> `184 passed`

## 下一步人工验收

1. 执行 `20260410_plan_d_stage_bindings.sql` 到开发数据库。
2. 手动提交一道视频题目。
3. 观察等待页是否出现 `solve`、`render_verify`，以及渲染失败时是否进入 `manim_fix`。
4. 如需验收失败态，可临时制造渲染错误并确认等待页文案与后端 `failedStage / errorCode` 一致。
