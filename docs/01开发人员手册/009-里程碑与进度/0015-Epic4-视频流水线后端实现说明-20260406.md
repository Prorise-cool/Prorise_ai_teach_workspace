# Epic 4 视频流水线后端实现说明

> 本文记录 `Epic 4` 当前仅后端范围的实现收口情况。事实状态仍以 [`_bmad-output/implementation-artifacts/`](../../../_bmad-output/implementation-artifacts/) 与 [`sprint-status.yaml`](../../../_bmad-output/implementation-artifacts/sprint-status.yaml) 为准。

## 范围说明

- 本次交付仅覆盖 FastAPI 后端，不包含 `student-web` 前端等待页、结果页与 mock handler 落地。
- 已完成并自测的后端 Story 范围：`4.2`、`4.3`、`4.4`、`4.5`、`4.6`、`4.9`。
- 已完成后端部分、但整体 Story 仍需前端收口：`4.1`、`4.10`。
- GitHub 关联 Issue：`#113 Epic 4 FastAPI 后端视频流水线与结果闭环`。
- 工作分支：`feature/epic-4-fastapi-video-pipeline-backend`。

## 已实现能力

- 视频流水线阶段与结果契约：
  - `understanding | storyboard | manim_gen | manim_fix | render | tts | compose | upload` 已在后端模型、SSE 事件与 `/api/v1/tasks/{task_id}/status` 中贯通。
  - 成功结果、失败结果、artifact graph、publish card 统一沉淀在 `packages/fastapi-backend/app/features/video/pipeline/models.py`。
- 流水线主链路：
  - `packages/fastapi-backend/app/features/video/tasks/video_task_actor.py` 已切到真实视频任务执行。
  - `packages/fastapi-backend/app/features/video/pipeline/services.py` 已串起题目理解、分镜生成、Manim 代码生成、自动修复、沙箱渲染、TTS、FFmpeg 合成、上传、结果回写、artifact graph 回写。
  - `packages/fastapi-backend/app/worker.py` 已注册真实视频任务工厂。
- 沙箱与资源治理：
  - `packages/fastapi-backend/app/features/video/pipeline/sandbox.py` 已提供 `LocalSandboxExecutor` 与 `DockerSandboxExecutor`，包含 AST 安全扫描、超时、OOM / 磁盘满错误映射。
  - `packages/fastapi-backend/docker/manim-sandbox/Dockerfile` 已补齐仓库内的 Manim 沙箱镜像构建入口。
  - 流水线成功/失败收口时会统一清理本地渲染、TTS、合成临时目录。
- 查询与发布 API：
  - `GET /api/v1/video/tasks/{task_id}/result`
  - `POST /api/v1/video/tasks/{task_id}/publish`
  - `DELETE /api/v1/video/tasks/{task_id}/publish`
  - `GET /api/v1/video/published`
- 契约与 mock 资产：
  - 已补齐 `contracts/video/v1/pipeline-stages.md`
  - 已补齐 `contracts/video/v1/video-result.schema.json`
  - 已补齐 `contracts/video/v1/video-failure.schema.json`
  - 已补齐 `contracts/video/v1/video-artifact-graph.md`
  - 已补齐 `contracts/video/v1/publish-api.md`
  - 已补齐对应 `mocks/video/v1/*.json`

## 自测结果

- 后端全量测试：
  - 命令：`pytest -q packages/fastapi-backend/tests`
  - 结果：`154 passed in 2.28s`
- 本次新增重点测试：
  - `packages/fastapi-backend/tests/unit/video/test_video_pipeline_models.py`
    - 覆盖 stage 区间、stage snapshot、脚本安全扫描、OOM / 磁盘满映射、临时目录清理。
  - `packages/fastapi-backend/tests/unit/video/test_video_pipeline_services.py`
    - 覆盖 understanding、storyboard、manim generation / fix、tts failover、compose 命令、upload retry、artifact graph。
  - `packages/fastapi-backend/tests/integration/test_video_pipeline_api.py`
    - 覆盖结果查询、公开发布、取消公开、公开列表 API 闭环。

## API 自测入口

- 结果详情：`GET /api/v1/video/tasks/{task_id}/result`
  - 预期返回 `VideoResultDetail`，成功态含 `result`，失败态含 `failure`。
- 公开发布：`POST /api/v1/video/tasks/{task_id}/publish`
  - 预期仅任务创建者可操作，成功后返回 `PublishedVideoCard`。
- 取消公开：`DELETE /api/v1/video/tasks/{task_id}/publish`
  - 预期幂等撤回公开状态。
- 公开列表：`GET /api/v1/video/published?page=1&pageSize=12`
  - 预期按 `publishedAt DESC` 返回分页卡片。

## 运行态与降级说明

- 本地 / 测试环境默认使用 `LocalAssetStore` 与 `stub-llm` / `stub-tts`，便于单测和集成测试稳定运行。
- `DockerSandboxExecutor` 在宿主机无 Docker 时自动回退到 `LocalSandboxExecutor`。
- FFmpeg 不可用或渲染产物为测试占位文件时，合成阶段会走 placeholder fallback。
- 视频主结果与 artifact graph 通过本地资产目录模拟对象存储引用；长期元数据仍通过 RuoYi 防腐层接口回写。

## 参考项目与许可证边界

- 参考来源：`references/manim-to-video-claw`
- 使用方式：
  - 仅参考其 FastAPI 视频流水线的阶段拆分、服务边界和修复链思路。
  - 当前仓库未直接复制其专有实现文件或大段代码。
- 许可证边界：
  - 该参考项目声明为 `Commercial License - Proprietary Software`。
  - 因此本仓库仅保留“流程借鉴”和“架构重组”的结果，不保留可识别的专有代码片段。
