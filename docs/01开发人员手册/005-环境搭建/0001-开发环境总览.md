# 开发环境总览

> **状态**: 已初始化
> **负责人**: 架构负责人 / 各模块 Owner
> **最后更新**: 2026-03-26

---

## 环境要求

### 推荐宿主环境

- macOS / Linux / WSL2
- 可用内存 `>= 16 GB`
- 可用磁盘空间 `>= 30 GB`
- 稳定访问 GitHub、 Docker Hub 与常用包管理镜像

### 当前仓库现实基线

| 维度 | 当前状态 | 说明 |
|------|----------|------|
| GitHub 仓库 | ✅ 已存在 | 默认分支为 `master` |
| 管理前端基线 | ✅ 已存在 | `packages/ruoyi-plus-soybean-master/` |
| RuoYi 后端参考实现 | ✅ 已存在 | `packages/RuoYi-Vue-Plus-5.X/` |
| FastAPI 业务后端 | ⚠️ 仅占位 | `packages/fastapi-backend/` 目前只有说明文件 |
| React 学生端前台 | ✅ 已初始化 | `packages/student-web/` 已完成 Vite + React 19 开发骨架、pnpm workspace 接入与最小检查链 |

## 工具链概览

| 工具 | 版本要求 | 用途 |
|------|----------|------|
| Git | `>= 2.43` | 分支管理、 PR 协作 |
| Node.js | `>= 20.19.0` | 对齐现有 Soybean 前端基线 |
| pnpm | `>= 10.5.0` | Monorepo / 前端依赖管理 |
| Python | `>= 3.11` | FastAPI 业务后端 |
| JDK | `17` | RuoYi-Vue-Plus 后端 |
| Maven | `>= 3.9` | RuoYi 多模块构建 |
| Docker Engine | `>= 24` | 中间件与参考服务启动 |
| Docker Compose | `v2` | 启动 MySQL / Redis / MinIO 等 |

## IDE 配置

推荐至少具备以下能力：

1. Git 集成
2. TypeScript / Vue / React 语法支持
3. Python 解释器与格式化支持
4. Java / Maven 工程支持
5. Markdown 编辑与预览

建议插件：

- ESLint
- EditorConfig
- Docker
- GitHub Pull Requests
- Python
- Java Extension Pack / IntelliJ Maven 支持

## 环境变量

### 现有配置来源

| 位置 | 用途 | 说明 |
|------|------|------|
| `packages/ruoyi-plus-soybean-master/.env.dev` | 管理前端开发环境 | 默认后端指向 `http://localhost:8080` |
| `packages/RuoYi-Vue-Plus-5.X/ruoyi-admin/src/main/resources/application-dev.yml` | RuoYi 后端开发环境 | 定义 MySQL、 Redis 、 SnailJob 等连接 |
| `packages/student-web/.env.local` | React 学生端前台 | 当前创建后用于本地联调 |
| `packages/fastapi-backend/.env.defaults` | FastAPI 通用默认项 | 非敏感共享默认值 |
| `packages/fastapi-backend/.env.local` | FastAPI 本地联调覆盖 | 本地地址、认证代理加密配置与联调开关 |
| `packages/fastapi-backend/.env.staging` / `.env.production` | FastAPI 部署环境 | 预发 / 生产覆盖，建议由平台环境变量替代 |

## Story 1.1 开发前检查清单

1. 已完成 GitHub Flow 初始化并具备 PR 审查路径。
2. 本机具备 `Node 20.19+`、`pnpm 10.5+`、`JDK 17`、`Maven 3.9+`、`Python 3.11+`。
3. MySQL 与 Redis 可本地启动。
4. 已明确 `story 1-1` 的业务前端承载方案：
   - 默认方案：新增 `packages/student-web/`
   - 替代方案：修订架构后在现有前端项目承载
5. 团队对 RuoYi 是认证主宿主、 FastAPI 不是第二套后台达成一致。

## 当前阻塞项

1. `packages/student-web/` 已具备开发基础设施，下一步直接进入 Story 1.1 的 RuoYi 认证接口接入。
2. `packages/fastapi-backend/` 尚未完成最小脚手架，涉及 Story 1.3 之前可先以 RuoYi 认证闭环为主。
