---
文档编号: PRA-DEV-003-0001
文档名称: 系统架构总览
版本: v2.0.0
状态: Released
作者: Prorise AI Teach 研发团队
创建日期: 2026-04-25
最近修订: 2026-04-25
适用范围: 全体研发 / 架构师 / 测试 / 运维 / 产品
密级: 内部公开
---

| 版本 | 日期 | 修订内容 | 作者 | 评审 |
|------|------|----------|------|------|
| v0.1.0 | 2026-03-24 | 占位骨架 | 研发团队 | — |
| v1.0.0 | 2026-04-25 | arc42 + 4+1 + C4 三视图首版 | 研发团队 | 架构组 |
| v2.0.0 | 2026-04-25 | 重构为「图多讲解少 + 全程抽象」总览风格；移除全部模块路径与文件级引用，下沉至 006 模块开发指南 | 研发团队 | 架构组 |

---

## 1. 概述

### 1.1 目的

本文档以 **arc42** 为骨架，融合 **4+1 视图** 与 **C4 模型**，给出 Prorise AI Teach 平台的 **抽象级总览**。它只回答四个问题：**做什么 / 给谁用 / 由哪些容器组成 / 如何协作**。

> 任何文件级引用、模块内部结构、代码锚点 **不在本文档范围**，请到《006 模块开发指南》查阅。

### 1.2 阅读路径

```mermaid
flowchart LR
    A[本文档<br/>系统总览] --> B[0002 技术选型]
    A --> C[0003 数据模型]
    A --> D[0004 API 规范]
    A --> E[0005 安全架构]
    A --> F[006/* 模块开发指南]
    F --> G[文件 / 接口 / 实现细节]
    A --> H[008 部署与运维]
```

> 图 1-1：从总览出发的阅读路径。本文档是入口，**不承担细节**。

### 1.3 缩写

| 缩写 | 含义 |
|---|---|
| BMAD | Brief / Map / Architecture / Develop（项目研发流程） |
| SoT | Source of Truth |
| LLM / TTS / MLLM | 大语言 / 文本转语音 / 多模态大模型 |
| SSE | Server-Sent Events |
| ADR | Architecture Decision Record |

---

## 2. 引用文件

- 同级：《0002 技术选型决策记录》《0003 数据模型设计》《0004 API 设计规范》《0005 安全架构》
- 兄弟章节：《006 模块开发指南》《008 部署与运维》
- 外部标准：arc42 v8.2、4+1 View（Kruchten, 1995）、C4 Model（Simon Brown）、ISO/IEC/IEEE 42010、GB/T 8567-2006

---

## 3. 引言与目标（arc42 §1）

### 3.1 业务目标（一图概览）

```mermaid
mindmap
  root((Prorise<br/>AI Teach))
    教师
      5 分钟产出解题视频
      一题多镜可重渲
    学生
      浏览器观看回放
      AI 实时答疑
    教务
      多 Provider 统一管理
      配额与权限治理
    平台
      Provider 容错切换
      端到端可观测
```

> 图 3-1：业务目标心智图

### 3.2 质量树

```mermaid
flowchart LR
    Q[质量需求] --> R[可靠性]
    Q --> P[性能]
    Q --> S[安全]
    Q --> M[可维护]
    Q --> O[可观测]
    R --> R1[Provider failover]
    R --> R2[任务重试与时限]
    P --> P1[首段可见 < 90s]
    P --> P2[SSE 首字节 < 300ms]
    S --> S1[JWT + RBAC]
    S --> S2[沙箱隔离]
    M --> M1[Feature-first 拆分]
    M --> M2[文件行数硬限制]
    O --> O1[request_id 贯穿]
    O --> O2[结构化日志 + 指标]
```

> 图 3-2：顶层质量属性树

### 3.3 干系人地图

```mermaid
flowchart TB
    Sys((Prorise<br/>AI Teach))
    T[老师]:::user
    S[学生]:::user
    A[教务管理员]:::user
    D[研发团队]:::dev
    O[运维 / 安全]:::ops
    P[产品]:::biz

    T -->|视频生产| Sys
    S -->|视频消费 + 答疑| Sys
    A -->|平台治理| Sys
    D -->|演进与维护| Sys
    O -->|可用性 / 合规| Sys
    P -->|路线图| Sys

    classDef user fill:#dff,stroke:#06c
    classDef dev fill:#fed,stroke:#c60
    classDef ops fill:#dfd,stroke:#3a3
    classDef biz fill:#fdd,stroke:#c33
```

> 图 3-3：干系人与关注点

---

## 4. 约束条件（arc42 §2）

```mermaid
flowchart LR
    subgraph Tech[技术约束]
        T1[Python 3.11+]
        T2[Java 17 + RuoYi 5.x]
        T3[现代浏览器, 不兼容 IE]
    end
    subgraph Sec[安全约束]
        S1[资源走私桶, 不公开 CDN]
        S2[Provider 必须经统一抽象]
    end
    subgraph Proc[流程约束]
        P1[Story 必挂 BMAD Epic]
        P2[文件行数硬限制]
    end
    subgraph Dep[部署约束]
        D1[当前单机 Compose]
        D2[K8s 演进规划中]
    end
```

> 图 4-1：四类约束概览。任何违反约束的设计变更必须通过 ADR。

---

## 5. 上下文与范围（C4-Context, arc42 §3）

```mermaid
flowchart TB
    subgraph Users[使用方]
        T[老师]
        S[学生]
        A[教务管理员]
    end

    P((Prorise<br/>AI Teach))

    subgraph External[外部系统]
        L[LLM Providers]
        TS[TTS Providers]
        OSS[对象存储]
        SMS[短信网关]
        CDN[反向代理]
    end

    T -->|生产| P
    S -->|消费 / 答疑| P
    A -->|治理| P
    P -->|文本生成 / 视觉自修| L
    P -->|语音合成| TS
    P -->|视频 / 原图| OSS
    P -->|登录验证| SMS
    CDN -.HTTPS 反代.-> P
```

> 图 5-1：C4-Context — 系统与外部干系实体

**范围界定：**

- ✅ In-scope：教学视频生产、视频消费、AI 答疑、教务治理、Provider 路由、Auth / RBAC
- ❌ Out-of-scope：第三方 LLM 训练、社交互动、支付（规划中）

---

## 6. 解决方案策略（arc42 §4）

```mermaid
flowchart TB
    Style[架构风格<br/>模块化单体 + 异步 Worker]
    Split[拆分维度<br/>Feature-first]
    Async[异步策略<br/>队列驱动 + SSE 推送]
    Data[数据策略<br/>关系库 + 缓存 + 对象存储]
    AI[AI 接入<br/>Provider 抽象 + Failover]
    FE[前端策略<br/>双前端栈]
    Box[沙箱策略<br/>渲染隔离]
    Obs[观测策略<br/>request_id + 结构化日志]

    Style --> Split --> Async
    Async --> Data
    Async --> AI
    FE --> Async
    AI --> Box
    Async --> Obs
```

> 图 6-1：八大策略的总线图。**每条策略只回答"为什么这样选"，具体实现在 0002《技术选型决策记录》。**

---

## 7. 构建块视图（C4-Container, arc42 §5）

### 7.1 容器全景

```mermaid
flowchart TB
    subgraph Edge[Edge / 反代层]
        N[反向代理]
    end

    subgraph Frontend[前端]
        AF[管理后台 SPA<br/>Vue 3 Soybean]
        SF[学生端 SPA<br/>React + TypeScript]
    end

    subgraph Backend[后端]
        FA[FastAPI 主进程<br/>REST + SSE 编排]
        FW[异步 Worker<br/>视频与长任务]
        RJ[RuoYi 后台<br/>账号 / 权限 / 配额]
        SJ[定时任务调度]
    end

    subgraph Data[数据底座]
        MY[(关系库)]
        RE[(缓存 / 队列)]
        MI[(对象存储)]
    end

    N --> AF
    N --> SF
    N --> FA
    N --> RJ
    AF --> RJ
    SF --> FA
    SF --> RJ
    FA -.enqueue.-> RE
    FW -.consume.-> RE
    FA --> MY
    FW --> MY
    RJ --> MY
    FA --> MI
    FW --> MI
    FA -.鉴权.-> RJ
    FW -.鉴权.-> RJ
```

> 图 7-1：C4-Container — 7 类核心容器

### 7.2 容器职责一览

```mermaid
flowchart LR
    FA[FastAPI 主进程]
    FW[异步 Worker]
    RJ[RuoYi 后台]
    SJ[调度中心]
    AF[管理后台 SPA]
    SF[学生端 SPA]

    FA -->|做| F1[REST 编排]
    FA -->|做| F2[SSE 推送]
    FA -->|做| F3[Provider 调度]
    FW -->|做| W1[视频管线执行]
    FW -->|做| W2[Manim 渲染调用]
    FW -->|做| W3[TTS 合成调用]
    RJ -->|做| R1[账号 / RBAC]
    RJ -->|做| R2[Provider 配置]
    RJ -->|做| R3[资源 / 配额]
    SJ -->|做| S1[周期性任务]
    AF -->|做| A1[教务治理界面]
    SF -->|做| Sf1[学生学习界面]
    SF -->|做| Sf2[AI 答疑界面]
```

> 图 7-2：每个容器只做一件事

### 7.3 不在本文范围

| 关注点 | 文档位置 |
|---|---|
| 各容器内部组件 / 包结构 | 《006-模块开发指南》 |
| API 路由与契约 | 《0004 API 设计规范》 |
| 数据模型表设计 | 《0003 数据模型设计》 |
| 安全设计细节 | 《0005 安全架构》 |
| 部署拓扑细节 | 《008-部署与运维》 |

---

## 8. 运行时视图（4+1 Process View, arc42 §6）

### 8.1 关键场景一：老师生成视频

```mermaid
sequenceDiagram
    actor T as 老师
    participant FE as 前端
    participant FA as FastAPI
    participant RJ as RuoYi
    participant Q as 队列
    participant W as Worker
    participant L as LLM
    participant TT as TTS
    participant SB as 渲染沙箱
    participant O as 对象存储

    T->>FE: 提交题目
    FE->>RJ: 登录
    RJ-->>FE: 凭证
    FE->>FA: 创建任务（携凭证）
    FA->>RJ: 鉴权透传
    RJ-->>FA: 身份与权限
    FA->>Q: 入队
    FA-->>FE: 任务已受理
    FE->>FA: 订阅 SSE
    W->>Q: 消费
    W->>L: 理解 + 分镜
    L-->>W: 摘要 + 分段
    W-->>FA: 进度
    FA-->>FE: SSE: 理解完成
    loop 每个分段
        W->>L: 生成动画代码
        W->>SB: 渲染
        W->>TT: 合成语音
        W-->>FA: 段落完成
        FA-->>FE: SSE: 预览可见
    end
    W->>O: 上传产物
    W-->>FA: 任务完成
    FA-->>FE: SSE: 完成
```

> 图 8-1：视频生成端到端时序

### 8.2 关键场景二：学生 AI 答疑

```mermaid
sequenceDiagram
    actor S as 学生
    participant FE as 前端
    participant FA as FastAPI
    participant L as LLM
    participant DB as 关系库

    S->>FE: 输入问题
    FE->>FA: 发起对话（SSE）
    FA->>L: 流式推理
    loop 每个 token chunk
        L-->>FA: chunk
        FA-->>FE: SSE delta
    end
    FA->>DB: 落库会话
    FA-->>FE: SSE 完成
```

> 图 8-2：AI 答疑流式推理

### 8.3 失败与降级

```mermaid
flowchart TB
    F1[LLM Provider 5xx / 429] --> H1[健康标记不健康]
    H1 --> A1[切到次选 Provider]

    F2[渲染失败] --> A2[标记该段失败<br/>不阻塞其他段]

    F3[TTS 全失败] --> A3[静音输出 + warn]

    F4[Worker 超时] --> A4[终止 actor<br/>任务标失败]

    F5[队列不可达] --> A5[拒绝新任务<br/>返回服务不可用]
```

> 图 8-3：五条主降级路径

---

## 9. 部署视图（4+1 Physical View, arc42 §7）

### 9.1 单机部署拓扑

```mermaid
flowchart TB
    subgraph Internet
        U[用户浏览器]
    end

    subgraph Server[部署服务器]
        NX[反向代理]
        subgraph Compose[容器编排]
            AF[管理后台]
            SF[学生端]
            FA[FastAPI]
            FW[Worker]
            RJ[RuoYi]
            SJ[调度]
        end
        subgraph Stores[宿主卷]
            MY[(关系库)]
            RE[(缓存)]
            MI[(对象存储)]
        end
    end

    U -.HTTPS.-> NX
    NX --> AF
    NX --> SF
    NX --> FA
    NX --> RJ
    Compose --> Stores
```

> 图 9-1：当前生产形态（单机 Compose）

### 9.2 网络分区

```mermaid
flowchart LR
    subgraph EdgeNet[edge 网络]
        N[反向代理]
        AF[管理后台]
        SF[学生端]
    end
    subgraph BackNet[backend 网络]
        FA[FastAPI]
        FW[Worker]
        RJ[RuoYi]
        SJ[调度]
        MY[(关系库)]
        RE[(缓存)]
        MI[(对象存储)]
    end
    N --> FA
    N --> RJ
    AF --> N
    SF --> N
```

> 图 9-2：edge 与 backend 双网，数据底座不暴露给 edge

### 9.3 演进路线

```mermaid
gitGraph
    commit id: "v1: 单机 Compose"
    commit id: "v1.x: 数据卷外置"
    branch staging
    commit id: "无状态化清理"
    commit id: "Redis 角色拆分"
    branch k8s
    commit id: "v2: 容器化进 K8s"
    commit id: "HPA + Ingress"
```

> 图 9-3：从单机到 K8s 的两阶段演进

---

## 10. 横切关注点（arc42 §8）

```mermaid
flowchart TB
    Cross[横切关注点]
    Cross --> C1[配置]
    Cross --> C2[鉴权]
    Cross --> C3[错误处理]
    Cross --> C4[日志与观测]
    Cross --> C5[缓存]
    Cross --> C6[沙箱]
    Cross --> C7[幂等与限流]

    C1 -.分层 .env<br/>按环境覆盖.-> C1d[策略]
    C2 -.账号在 RuoYi<br/>鉴权下沉 FastAPI.-> C2d[策略]
    C3 -.统一错误模型<br/>request_id 贯穿.-> C3d[策略]
    C4 -.结构化 JSON<br/>+ 队列指标.-> C4d[策略]
    C5 -.缓存以 Redis 为主<br/>新增热点走 ADR.-> C5d[策略]
    C6 -.渲染必走沙箱<br/>静态扫描禁危险调用.-> C6d[策略]
    C7 -.幂等键 + 漏桶<br/>统一中间件.-> C7d[策略]
```

> 图 10-1：横切关注点全景。**每点的实现细节见对应专题文档**：鉴权 → 0005 安全；错误模型 → 0004 API；缓存与日志 → 008 运维。

---

## 11. 架构决策记录（arc42 §9）

| ID | 标题 | 状态 | 详见 |
|---|---|---|---|
| ADR-001 | 后端选 FastAPI（异步 / 类型 / OpenAPI） | Accepted | 0002 §3.1 |
| ADR-002 | 异步队列选 Dramatiq（轻量 / Redis broker） | Accepted | 0002 §3.2 |
| ADR-003 | 视频管道全量重写（替换历史实现） | Accepted | 0002 §3.3 |
| ADR-004 | 双前端栈：React 学生端 + Vue 管理后台 | Accepted | 0002 §3.4 |
| ADR-005 | MemPalace 作为 AI 记忆与规范唯一入口 | Accepted | 0002 §3.5 |
| ADR-006 | 多 Provider 路由（含 LLM / TTS / MLLM） | Accepted | 0002 §3.6 |
| ADR-007 | 渲染走 Docker 沙箱 | Accepted | 0002 §3.7 |
| ADR-008 | BMAD（Epic→Story→Implementation） | Accepted | 0002 §3.8 |

---

## 12. 质量需求（arc42 §10）

```mermaid
flowchart LR
    subgraph 性能
        P1[首段可见 P50 < 90s]
        P2[API P95 < 500ms]
    end
    subgraph 可靠
        R1[Provider 故障下成功率 ≥ 99.5%]
        R2[Worker 超时不外溢]
    end
    subgraph 安全
        S1[全接口必须 Bearer]
        S2[渲染脚本静态扫描 100% 拦截]
    end
    subgraph 可观测
        O1[5xx 必能凭 request_id 还原]
    end
    subgraph 可维护
        M1[文件行数硬限]
        M2[Feature-first 边界]
    end
```

> 图 12-1：核心质量场景一图概览

---

## 13. 风险与技术债（arc42 §11）

```mermaid
flowchart TB
    Risk[风险与技术债]
    Risk --> R1[单机部署 / 无水平扩展]
    Risk --> R2[Redis 角色双重承担]
    Risk --> R3[对象存储自托管]
    Risk --> R4[Provider 计费与配额]
    Risk --> R5[渲染失败排障人工化]
    Risk --> R6[FastAPI / RuoYi 双栈成本]

    R1 -.K8s 化路线.-> M1[缓解]
    R2 -.拆分两套实例.-> M2[缓解]
    R3 -.评估云对象存储.-> M3[缓解]
    R4 -.配额墙 + 用量审计.-> M4[缓解]
    R5 -.失败样本归档检索.-> M5[缓解]
    R6 -.双栈边界明确.-> M6[缓解]
```

> 图 13-1：六大风险与对应缓解路径

---

## 14. 术语表（arc42 §12）

| 术语 | 含义 |
|---|---|
| Section | 视频中一段独立动画 + 旁白 |
| Codebook | 用于辅助 LLM 生成动画代码的模板库 |
| Preview | 管线未结束但首段已可播放的状态 |
| Provider Binding | 把业务模块绑定到具体 Provider 的配置形态 |
| Sandbox | 渲染脚本的隔离执行环境 |
| Idempotency Key | API 防重提交标识 |
| Failover | Provider 故障时切换到次选的能力 |
| SLI / SLO | 服务等级指标 / 目标 |

---

## 附录：参考资料

- arc42 — <https://arc42.org>
- C4 Model — <https://c4model.com>
- 4+1 View — Kruchten, IEEE Software, Nov 1995
- ISO/IEC/IEEE 42010 软件架构描述
- GB/T 8567-2006 计算机软件文档编制规范

---

> **本文为「总览」性质：图说优先、抽象优先、零路径引用。** 任何「这个东西具体在哪个文件」「内部由哪些类组成」「字段如何定义」「环境变量名怎么写」一律请到对应专题文档查阅；本文不收录。
