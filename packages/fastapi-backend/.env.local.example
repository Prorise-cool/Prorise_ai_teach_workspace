# 仅用于本机开发机，不进入预发/生产。

# FastAPI 运行环境。
FASTAPI_ENV=development
# 本地调试启用热更新。
FASTAPI_RELOAD=true
# 本地 RuoYi 地址。
FASTAPI_RUOYI_BASE_URL=http://127.0.0.1:8080
# Provider 配置来源；本地联调可按需切到 ruoyi。
FASTAPI_PROVIDER_RUNTIME_SOURCE=ruoyi
