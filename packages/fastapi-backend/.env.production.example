# 复制为 `.env.production`，用于生产环境覆盖。

# FastAPI 运行环境。
FASTAPI_ENV=production
# 生产环境关闭热更新。
FASTAPI_RELOAD=false
# 生产 RuoYi 地址。
FASTAPI_RUOYI_BASE_URL=https://ruoyi.example.com
# Provider 配置来源；生产通常从 RuoYi 读取。
FASTAPI_PROVIDER_RUNTIME_SOURCE=ruoyi
