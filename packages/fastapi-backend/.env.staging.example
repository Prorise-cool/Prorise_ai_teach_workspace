# 复制为 `.env.staging`，用于预发环境覆盖。

# FastAPI 运行环境。
FASTAPI_ENV=staging
# 预发环境关闭热更新。
FASTAPI_RELOAD=false
# 预发 RuoYi 地址。
FASTAPI_RUOYI_BASE_URL=https://staging-ruoyi.example.com
# Provider 配置来源；预发通常从 RuoYi 读取。
FASTAPI_PROVIDER_RUNTIME_SOURCE=ruoyi
