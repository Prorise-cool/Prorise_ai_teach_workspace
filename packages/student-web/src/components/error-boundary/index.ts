/**
 * 文件说明：ErrorBoundary 统一导出入口。
 *
 * 业务代码建议从 `@/components/error-boundary` 引入，
 * 便于未来切换实现（例如接入 Sentry）。
 */
export { ErrorBoundary } from './ErrorBoundary';
export type { ErrorBoundaryProps } from './ErrorBoundary';
